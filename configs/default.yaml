monitor_interval: 5
process_interval: 1
http_port: 8090
log_dir: /var/lib/cluebot # ./logs
pid_file: /run/cluebot.pid # ./cluebot.pid
services:
  - docker
  - harbrixd
thresholds:
  cpu_alert: 90
  memory_alert: 90
  disk_alert: 90
  process_limit: 5000
